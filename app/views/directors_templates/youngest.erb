<h1>Youngest Director</h1>

<% if @youngest_director.present? %>
  <p>The youngest director in our dataset right now is 
    <a href="/directors/<%= @youngest_director.id %>">
      <%= @youngest_director.name %>
    </a>, born on <%= @youngest_director.dob.strftime("%B %d, %Y") %>.
  </p>
<% else %>
  <p>No directors found in the database.</p>
<% end %>


<a href="/directors/eldest">See Eldest Director</a>
<a href="/">Home</a>
